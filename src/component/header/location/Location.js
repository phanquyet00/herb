import React from 'react'
import styled from "styled-components";
import { Button } from '@mui/material';

const AdressButton = styled(Button)`
`


const Location = () => {
  return (
        <AdressButton>địa điểm</AdressButton>
  )
}

export default Location
