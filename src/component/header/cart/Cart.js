import React from 'react'
import styled from 'styled-components'
import { Button } from "@mui/material"

const CartButton = styled(Button)`
`

const Cart = () => {
  return (
      <CartButton>giỏ hàng</CartButton>
  )
}

export default Cart
