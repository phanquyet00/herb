import React from 'react'
import styled from 'styled-components'
import { Button } from '@mui/material'

const AccountSig = styled(Button)``

const Account = () => {
  return (
    <div>
        <AccountSig>đăng nhập</AccountSig>
    </div>
  )
}

export default Account
